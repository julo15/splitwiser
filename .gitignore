venv/
__pycache__/
*.pyc
*.log
*.db
.env
db.sqlite3
node_modules/
dist/
.DS_Store
frontend/node_modules/
frontend/dist/
backend/venv/
backend/__pycache__/
backend/data/

# Secrets (Google Cloud credentials, etc.)
secrets/
.pytest_cache/
.coverage
htmlcov/
.claude/
.agents/reviews/
# The backend's receipt store. Anchored with a leading slash: a bare `data/`
# matches at any depth, and silently swallowed frontend/src/data/ — the app
# built locally and failed in Docker because the file was never committed.
/data/
backend/data/
credentials.json
